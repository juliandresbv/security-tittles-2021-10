require('dotenv').config();

//https://mochajs.org
//https://www.chaijs.com/guide/styles/#assert
const assert = require('chai').assert;



describe('test', ()=>{

  before(async () => {
  });

  after(async () => {
  });

  it('', async ()=>{
    assert.isTrue(true);
  });

});